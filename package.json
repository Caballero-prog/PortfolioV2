{
  "name": "portfolio",
  "version": "1.0.0",
  "private": true,
  "homepage": "https://Caballero-prog.github.io/Portfolio",
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "predeploy": "pnpm run build",
    "deploy": "gh-pages -d build"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
